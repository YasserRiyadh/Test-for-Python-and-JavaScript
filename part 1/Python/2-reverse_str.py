def reverse_string(s: str) -> str:
    text = 'Hello World'
    reversed_text = text[::-1] # Reverse the given text and store it in a new variable.
    
    print(reversed_text)
    return reversed_text

reverse_string('')